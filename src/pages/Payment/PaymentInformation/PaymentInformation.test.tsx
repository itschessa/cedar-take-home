describe("PaymentInformation", () => {
  it("does not continue to Payment Review if there are invalid form fields", async () => {});

  it("navigates to Payment Review on button click if all form fields are valid", async () => {});

  it("updates payment state when form fields change", async () => {});

  it("displays Edit button when section is collapsed", async () => {});

  it("does not display Edit button when section is expanded", async () => {});

  it("expands section when Edit button is clicked", async () => {});
});
