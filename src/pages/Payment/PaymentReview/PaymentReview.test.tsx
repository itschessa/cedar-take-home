import { render } from "@testing-library/react";
import { PaymentReview } from "./PaymentReview";

describe("PaymentReview", () => {
  it("renders", () => {
    // render(<PaymentReview />);
  });

  it("submits payment on button click", () => {});

  it("displays loading message while submitting payment", () => {});

  it("displays error message if payment fails", () => {});

  it("navigates to Thank You page on successful payment", () => {});
});
